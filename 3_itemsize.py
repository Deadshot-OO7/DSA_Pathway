
from array import *

arr = array('i', [1, 2, 3, 4, 5, 6])
charArr = array('Q', [3, 3])
print(arr)

print(charArr.itemsize)
# itemsize attribute return
# the size of the dtype of the item of the underlying data for the given Series object.

